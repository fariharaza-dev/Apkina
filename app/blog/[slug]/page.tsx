import {notFound} from "next/navigation";
import {StoreShell,Breadcrumbs} from "@/components/store-shell";
import {posts} from "@/lib/journal";
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=posts.find(p=>p.slug===slug);return {title:p?`${p.title} | Apkina Journal`:"Article not found | Apkina",description:p?.excerpt}}
export default async function Article({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=posts.find(p=>p.slug===slug);if(!p)notFound();return <StoreShell><article className="section-container journal-article"><Breadcrumbs items={[{label:"Journal",href:"/blog"},{label:p.category}]}/><header><p className="eyebrow">{p.category} · {p.read} · Sample article</p><h1>{p.title}</h1><p>{p.excerpt}</p><span>By the Apkina team</span></header><img className="article-cover" src={`/assets/${p.image}.png`} alt="Behind the scenes of a creator’s studio shoot" width="1440" height="900"/><div className="article-body">{p.sections.map((s,i)=><section key={s.title}><span className="eyebrow">0{i+1}</span><h2>{s.title}</h2><p>{s.text}</p></section>)}<div className="article-end"><h2>Find the tools for your next idea.</h2><a className="pill dark" href={`/category/${p.link}`}>Explore the collection ↗</a><a href="/blog">← Back to the journal</a></div></div></article></StoreShell>}
