import {StoreShell,Breadcrumbs} from "@/components/store-shell";
import {posts} from "@/lib/journal";
export const metadata={title:"The Creator’s Journal | Apkina",description:"Sample stories, practical ideas and gear notes for your next creation."};
export default function Blog(){return <StoreShell><section className="section-container journal"><Breadcrumbs items={[{label:"Journal"}]}/><header className="journal-heading"><p className="eyebrow">THE APKINA JOURNAL</p><h1>Good ideas.<br/><span>Better creations.</span></h1><p>Practical notes on light, sound and the tools behind your next story.</p><span className="sample-label">4 sample articles</span></header><a className="journal-feature" href={`/blog/${posts[0].slug}`}><img src={`/assets/${posts[0].image}.png`} alt="Creator filming in a colourfully lit studio" width="1440" height="900"/><div><p className="eyebrow">{posts[0].category} · {posts[0].read}</p><h2>{posts[0].title}</h2><p>{posts[0].excerpt}</p><span className="pill light">Read the story ↗</span></div></a><div className="journal-grid">{posts.slice(1).map((p,i)=><article key={p.slug}><a className={`journal-card-image journal-image-${i}`} href={`/blog/${p.slug}`}><img src={`/assets/${p.image}.png`} alt={i===1?"Lighting and recording on a studio set":"Camera creator at work in a studio"} width="1440" height="900" loading="lazy"/><span>0{i+2}</span></a><p className="eyebrow">{p.category} · {p.read}</p><a href={`/blog/${p.slug}`}><h2>{p.title}</h2></a><p>{p.excerpt}</p><a className="read-story" href={`/blog/${p.slug}`}>Read story ↗</a></article>)}</div></section></StoreShell>}
